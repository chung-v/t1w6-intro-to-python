# get the length from the user
length = float(input("Enter the length of the sqaure: "))
# print(length)
# calculate and store the area in a variable, are = length * length
area = length * length
# print the calculated area to the terminal
print(area)
print(f"The calculated area is {area}")